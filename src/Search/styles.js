const styles = theme => ({
	margin: {
		height: theme.spacing(3),
	},
	textField: {
		marginLeft: theme.spacing(1),
		marginRight: theme.spacing(1),
		width: 250,
	},

	container: {
		marginTop: '50px'
	},

	resultItem: {
		padding: '20px'
	}
});

export default styles;
